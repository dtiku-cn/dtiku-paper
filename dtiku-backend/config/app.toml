[web]
port = 8000
graceful = true

[web.middlewares]
static = { enable = true }

[logger]
# level = "debug"
pretty_backtrace = true

[sqlx]
uri = "${SRC_DATABASE_URL}"

[sea-orm]
uri = "${DATABASE_URL}"

[stream]
uri = "redis://localhost"

[redis]
uri = "redis://localhost"

[huggingface]
cache_dir = "${HF_HOME../.hf-cache}"